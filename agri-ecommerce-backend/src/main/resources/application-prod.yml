spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN

app:
  auth:
    password-reset:
      expose-token: false
  public:
    base-url: ${APP_PUBLIC_BASE_URL:https://agrimarket.example.com}
